# DDoS Protection Configuration

# API Keys
OPENAI_API_KEY: 'your-api-key-here'

# Time settings
BLOCK_TIME: 300  # seconds
TIME_WINDOW: 60  # seconds

# Thresholds
MAX_CONNECTIONS_PER_IP: 100
SYN_FLOOD_THRESHOLD: 100
UDP_FLOOD_THRESHOLD: 1000
ICMP_FLOOD_THRESHOLD: 50
HTTP_FLOOD_THRESHOLD: 200
PACKET_RATE_THRESHOLD: 1000  # packets per second
BANDWIDTH_THRESHOLD: 10485760  # 10 MB/s in bytes

# New AI action thresholds
RATE_LIMIT_THRESHOLD: 50  # connections per minute
TEMPORARY_BLOCK_TIME: 600  # seconds (10 minutes)
CAPTCHA_CHALLENGE_THRESHOLD: 30  # suspicious requests
ALERT_THRESHOLD: 20  # suspicious activities

# Whitelist
WHITELIST:
  - '192.168.1.1'
  - '10.0.0.1'

# AI prompt template
AI_PROMPT_TEMPLATE: >
  You are a cybersecurity expert. Analyze the following network traffic data and suggest appropriate actions to mitigate potential threats.
  Consider the following options:
  1. Block IP (for severe threats)
  2. Temporary block (for moderate threats, {TEMPORARY_BLOCK_TIME} seconds)
  3. Rate limit (for suspicious activity, limit to {RATE_LIMIT_THRESHOLD} connections per minute)
  4. CAPTCHA challenge (for potential bot activity)
  5. Add to watchlist (for mild suspicion)
  6. Send alert (for unusual patterns)
  7. No action (if behavior seems normal)
  Provide your recommendation as a single word (e.g., "block", "temp_block", "rate_limit", "captcha", "watchlist", "alert", "none"),
  followed by a brief explanation.
